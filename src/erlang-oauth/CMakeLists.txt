SET(OAUTH_PREFIX ${CMAKE_ERL_LIB_INSTALL_PREFIX}/erlang-oauth)

CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/src/oauth.app.src
               ${CMAKE_CURRENT_BINARY_DIR}/oauth.app)

INSTALL(DIRECTORY ${COUCHDB_DEFAULT_LIB_DIR}/oauth/ebin
        DESTINATION ${OAUTH_PREFIX})
