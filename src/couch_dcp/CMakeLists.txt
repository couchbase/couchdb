SET(COUCH_DCP_VERSION "1.0.0")
SET(COUCH_DCP_PREFIX ${CMAKE_ERL_LIB_INSTALL_PREFIX}/couch_dcp-${COUCH_DCP_VERSION})

INSTALL(DIRECTORY ${COUCHDB_DEFAULT_LIB_DIR}/couch_dcp/ebin
        DESTINATION ${COUCH_DCP_PREFIX})

ADD_SUBDIRECTORY(test)
